@model IEnumerable<Momon.Biju.Web.Models.SubCategoryViewModel>

@{
    ViewBag.Title = "Sub categoria";
    Layout = "_Layout";
}

<h2>Sub categorias</h2>

<a asp-action="Create" class="btn btn-secondary">Nova sub categoria</a>

<table class="table">
    <thead>
    <tr>
        <th scope="col">@Html.DisplayNameFor(model => model.Name)</th>
        <th scope="col"></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a>
            </td>
        </tr>
    }
    </tbody>
</table>  
